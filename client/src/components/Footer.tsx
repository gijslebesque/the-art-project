import React from 'react';
import '../styles/footer.scss';

const footer = (props:any) => {
    return(
        <footer>
            <h2>Contact</h2>
        </footer>
    );
}

export default footer;